const theName = 0;

if (theName) {
    console.log(theName);
}

//to check with if statements, I must always check against something
// Example:
const totalIncome = 0;

// Must be explicity when using if statements
if (totalIncome !== 0) {
    console.log(totalIncome);
}

